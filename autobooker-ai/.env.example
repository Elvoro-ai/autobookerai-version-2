# Application URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# PostgreSQL connexion string utilisée par Prisma. Modifiez l’hôte pour correspondre à votre base.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/autobooker?schema=public

# Secret pour l’authentification (Auth.js). Remplacez par une valeur générée de manière aléatoire.
AUTH_SECRET=changeme

# API key Resend pour l’envoi d’e‑mails (https://resend.com). Utilisé dans l’API `api/notify/send`.
RESEND_API_KEY=
# Adresse e‑mail utilisée comme expéditeur pour les notifications.
RESEND_SENDER=no-reply@autobooker.ai

# Twilio (SMS) – optionnel. Laissez vide si vous n’utilisez pas les SMS.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# Paramètres de la base PostgreSQL embarquée (docker-compose). Ils doivent correspondre à `POSTGRES_USER`, `POSTGRES_PASSWORD` et `POSTGRES_DB`.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=autobooker
